import { QuizProvider } from 'context/quiz';

const App = () => {
	const quizProviderValues = {
    
  };

	return <QuizProvider value={quizProviderValues}>App</QuizProvider>;
};

export default App;
